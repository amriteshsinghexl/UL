# Full backend dependencies (model engine + FastAPI web layer)
# Install with: pip install -r requirements-backend.txt

# --- Core model dependencies (from requirements.txt) ---
-r requirements.txt

# --- Web layer ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic-settings>=2.0.0
openpyxl>=3.1.0
